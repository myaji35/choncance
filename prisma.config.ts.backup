import { defineConfig } from "prisma/config";

// Load from environment variables - GCP Cloud SQL
const DATABASE_URL = process.env.DATABASE_URL || "postgresql://postgres:VinteeDB2025!@34.64.209.227:5432/vintee?sslmode=disable";
const DIRECT_URL = process.env.DIRECT_URL || "postgresql://postgres:VinteeDB2025!@34.64.209.227:5432/vintee?sslmode=disable";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  engine: "classic",
  datasource: {
    url: DATABASE_URL,
    directUrl: DIRECT_URL,
  },
});
