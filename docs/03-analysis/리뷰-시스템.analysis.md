# 리뷰-시스템 Analysis Report (v3.0 - 재구현 후 분석)

> **Analysis Type**: Gap Analysis (Design vs Implementation)
> **Project**: VINTEE (빈티)
> **Analyst**: bkit-gap-detector (Claude Opus 4.6)
> **Date**: 2026-04-06
> **Design Doc**: docs/02-design/features/리뷰-시스템.design.md
> **Previous**: v2.0 (2026-03-01, 93%), v1.0 (2026-03-01, 72%)

---

## 1. Overall Match Rate: 92%

```
+-------------------------------------------------+
|  Overall Match Rate: 92%                         |
+-------------------------------------------------+
|  MATCH (Full):       26 items (87%)              |
|  PARTIAL:             3 items (10%)              |
|  MISSING:             1 items ( 3%)              |
+-------------------------------------------------+
```

## 2. Category Scores

| 카테고리 | 스코어 | 상태 |
|----------|:------:|:----:|
| API Endpoints | 88% | PASS |
| 데이터 모델 | 100% | PASS |
| 컴포넌트 | 95% | PASS |
| 페이지 | 100% | PASS |
| 비즈니스 로직 | 90% | PASS |
| 유틸리티/스키마 | 90% | PASS |
| **전체** | **92%** | **PASS** |

---

## 3. Gap Analysis Details

### 3.1 API Endpoints

| 설계 엔드포인트 | 구현 파일 | 상태 | 비고 |
|----------------|-----------|------|------|
| `POST /api/reviews` | `src/app/api/reviews/route.ts` | MATCH | Zod 검증, 트랜잭션, 에러 코드 일치 |
| `GET /api/properties/[id]/reviews` | `src/app/api/properties/[id]/reviews/route.ts` | MATCH | 페이지네이션, summary, maskName 적용 |
| `POST /api/reviews/[id]/reply` | `src/app/api/reviews/[id]/reply/route.ts` | MATCH | 호스트 소유 확인, 중복 방지 |
| `GET /api/host/reviews` | SSR 직접 쿼리 | PARTIAL | API route 미존재, Server Component에서 Prisma 직접 호출 |

### 3.2 컴포넌트

| 설계 컴포넌트 | 구현 파일 | 상태 | 비고 |
|--------------|-----------|------|------|
| `StarRating` | `src/components/review/StarRating.tsx` | MATCH | 독립 컴포넌트 분리 완료, hover/클릭 |
| `ReviewCard` | `src/components/review/ReviewCard.tsx` | MATCH | 호스트 답글 섹션, isHost/onReply |
| `ReviewList` | `src/components/review/ReviewList.tsx` | MATCH | SSR 초기 데이터 + "더 보기" |
| `HostResponseForm` | `src/components/review/HostResponseForm.tsx` | MATCH | 10-300자, onCancel 추가 |

### 3.3 페이지

| 설계 페이지 | 구현 파일 | 상태 |
|------------|-----------|------|
| `/bookings/[id]/review` | `page.tsx` + `ReviewPageClient.tsx` | MATCH |
| `/property/[id]` 리뷰 섹션 | `property/[id]/page.tsx` | MATCH |
| `/host/reviews` | `page.tsx` + `HostReviewsClient.tsx` | MATCH |
| `/bookings/[id]` 리뷰 버튼 | `bookings/[id]/page.tsx` | MATCH |

### 3.4 비즈니스 로직 (13개)

| 로직 항목 | 상태 | 구현 위치 |
|----------|------|-----------|
| 별점 1-5 필수 | MATCH | `validations/review.ts` |
| 텍스트 10-500자 | MATCH | `validations/review.ts` |
| 1인 1리뷰 (bookingId unique) | MATCH | DB 제약 + API 409 |
| COMPLETED 상태만 | MATCH | API 422 |
| 체크아웃 후에만 | MATCH | API 422 |
| 예약 소유권 확인 | MATCH | API 403 |
| 한국어 에러 메시지 | MATCH | 모든 에러 한국어 |
| 이름 마스킹 | MATCH | `properties/[id]/reviews/route.ts` |
| 평균 별점 소수점 1자리 | MATCH | `formatRating()` |
| 최신순 정렬 | MATCH | `orderBy: { createdAt: "desc" }` |
| 빈 상태 UI | MATCH | "아직 후기가 없습니다" |
| 답글 1회 제한 | MATCH | API 409 |
| 답글 10-300자 | MATCH | Zod 검증 |

### 3.5 유틸리티/스키마

| 설계 항목 | 구현 | 상태 |
|----------|------|------|
| `maskName()` | `lib/utils/review.ts` | MATCH |
| `formatRating()` | `lib/utils/review.ts` | MATCH |
| `canWriteReview()` | `lib/utils/review.ts` | MATCH |
| `reviewSchema` | `createReviewSchema` (이름 변경) | PARTIAL |
| `hostReplySchema` | `lib/validations/review.ts` | MATCH |

---

## 4. Differences Found

### 4.1 Missing (설계 O, 구현 X) — 1건

| 항목 | 설명 |
|------|------|
| `GET /api/host/reviews` API 라우트 | SSR 직접 쿼리로 대체 (프로젝트 패턴 일치) |

### 4.2 Changed (설계 != 구현) — 6건

| 항목 | 설계 | 구현 | 영향도 |
|------|------|------|--------|
| Zod 스키마 이름 | `reviewSchema` | `createReviewSchema` | Low |
| bookingId 검증 | `z.string().cuid()` | `z.string().min(1)` | Low |
| snsShareConsent | 미포함 | Zod/API에 추가 | Low |
| StarRating 소수점 | 반별 표시 | `Math.round()` 반올림 | Low |
| 성공 피드백 | 토스트 표시 | 페이지 리다이렉트만 | Low |
| `/api/reviews` GET 마스킹 | 마스킹 필수 | `guestName: r.user.name` (미적용) | Medium |

### 4.3 Added (설계 X, 구현 O) — 3건

| 항목 | 설명 |
|------|------|
| `GET /api/reviews` 범용 엔드포인트 | propertyId 필터 + 페이지네이션 |
| `ReviewForm` 독립 컴포넌트 | 재사용 가능한 폼 분리 |
| SNS 크레딧 지급 시스템 | snsShareConsent 동의 시 1000 크레딧 |

---

## 5. Recommended Actions

### P1 - 즉시 수정

| # | 항목 | 파일 | 설명 |
|---|------|------|------|
| 1 | `/api/reviews` GET 이름 마스킹 | `api/reviews/route.ts:133` | `r.user.name` → `maskName(r.user.name)` |

### P2 - 선택

| # | 항목 | 설명 |
|---|------|------|
| 1 | StarRating 소수점(반별) 표시 | 읽기 전용 half-star SVG |
| 2 | bookingId `z.string().cuid()` 강화 | 설계 원안 복원 |

### P3 - 향후

| # | 항목 | 설명 |
|---|------|------|
| 1 | `GET /api/host/reviews` API 라우트 분리 | 클라이언트 사이드 호출 필요 시 |
| 2 | ReviewStatus enum | 리뷰 숨김/관리 기능 확장 시 |

---

## 6. Conclusion

**Match Rate 92% >= 90% 기준 달성. Check 단계 통과.**

v2.0 대비 주요 개선:
- StarRating 독립 컴포넌트 분리 완료 (v2.0에서 P2 backlog)
- ReviewList "더 보기" 페이지네이션 구현 완료 (v2.0에서 P1)
- Next.js 16 + Prisma 7 최신 스택으로 재구현

다음 단계: P1 이슈 수정 후 `/pdca report 리뷰-시스템`

---

## Version History

| Version | Date | Match Rate | Author |
|---------|------|:----------:|--------|
| 1.0 | 2026-03-01 | 72% | bkit-gap-detector |
| 2.0 | 2026-03-01 | 93% | bkit-gap-detector |
| 3.0 | 2026-04-06 | 92% | bkit-gap-detector (Claude Opus 4.6) |
